<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/home"
        android:title="Home"
        android:icon="@drawable/ic_home_selector"
        app:showAsAction="always"/>

    <item android:id="@+id/profile"
        android:title="Profile"
        android:icon="@drawable/ic_user_selector"
        app:showAsAction="always"/>

    <item android:id="@+id/exams"
        android:title="Exams"
        android:icon="@drawable/ic_exam_selector"
        app:showAsAction="always"/>

    <item android:id="@+id/links"
        android:title="Links"
        android:icon="@drawable/ic_folder_selector"
        app:showAsAction="always"/>

    <item android:id="@+id/schedule"
        android:title="Schedule"
        android:icon="@drawable/ic_calendar_selector"
        app:showAsAction="always"/>

</menu>